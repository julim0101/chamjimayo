# 참지마요 — 경량 구성 (ML 모델 없이 동작)
#
# Tier 2 독성 분류 → 어휘 사전 폴백
# RAG 검색        → 문자 n-gram BM25 폴백
#
# Tier 1 정량 지표는 순수 연산이므로 **완전히 동일하게** 동작합니다.
# 메모리 1GB 제한이 있는 Streamlit Community Cloud 배포용.

# config.toml 이 [theme.light]/[theme.dark] 분리, borderColor, baseRadius,
# chartCategoricalColors, [[theme.fontFaces]] 를 쓴다. 1.58 미만에서는
# 알 수 없는 config 키로 취급되어 앱이 기동하지 않는다.
streamlit>=1.58,<2
altair>=5.0              # ui.heatmap — streamlit 이 함께 설치하지만 명시적으로 고정
pandas>=2.0
numpy>=1.24
matplotlib>=3.7          # pandas Styler.background_gradient 에 필요
